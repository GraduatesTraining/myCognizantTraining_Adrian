div.mainContent
  form.formLogin(name="loginForm" ng-controller="FormLoginCtrl as formLogin" novalidate)
    div(layout="row" layout-align="center center")
      img.logo(src="images/cognizantLogo.png")
    // Login Fields
    md-input-container.md-block
      label User
      input.formFields(type='text' ng-model="user" required)
    md-input-container.md-block
      label Password
      input.formFields(type='password' ng-model="password" required)
    // Register fields
    div(ng-show="formLogin.regFields")
      md-input-container.md-block
        label Name
        input.formFields(type='text' ng-model="name" required)
      md-input-container.md-block
        label Surname
        input.formFields(type='password' ng-model="surname" required)
    // Login buttons
    div(ng-hide="formLogin.regFields" layout="column" layout-align="center center")
      div.buttons
        md-content.buttons2
          md-button.logBut(ng-click="formLogin.login(user,password)") LOGIN
      div.buttons
        md-content.buttons2
          md-button.regBut(ng-click="formLogin.goRegister()") REGISTER
    // Register buttons
    div(ng-show="formLogin.regFields" layout="column" layout-align="center center")
      div.buttons
        md-content.buttons2
          md-button.regBut(ng-click="formLogin.register(user,password,name,surname)" ng-disabled="!loginForm.$valid") REGISTER
      div.buttons
        md-content.buttons2
          md-button.logBut(ng-click="formLogin.goLogin()") CANCELAR
    // Error messages
    div.errorMessages(layout="row" layout-align="center center" ng-show="formLogin.loginError || formLogin.registerError")
      // Login failed
      div(ng-show="formLogin.loginError") Login failed, email or password incorrect
      // Register failed
      div(ng-show="formLogin.registerError") This username exist. Choose another one
